#   测试程序目录=测试程序源文件夹名_工程名
SET(TEST_NAME ${TEST}_${PROJECT_NAME})
SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/${TEST_NAME})

#   链接选项
IF(${LIBRARY_TYPE} STREQUAL static)
    SET(LIBRARY_NAME lib${LIBRARY_NAME}.a)
ENDIF()

#
#   测试程序列表
#   e.g.    ADD_EXECUTABLE(${TARGET} ${SOURCE})
#           TARGET_LINK_LIBRARIES(${TARGET} ${LIBRARY_NAME})
#			ADD_TEST(NAME ${TARGET} COMMAND ${TARGET})
#
